version: "3.9"

services:
  dagsterdev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: dagster-pipeline-dev
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app